1. The permisions after the command is rw-r-xrw-.
2. The permisions after the command is -wxr-x-w-.
3. Find *.java.
4. It did not excecute successfully beacuse 127
 means the previous command was not found.
5. less
6. d (meaning its a directory) then goes r (meaning read)
7. chmod u+x ~/beginning.sh
8. It copies the entire directory to the destination account.
Since it does not exit it creates it.
9. tar xvf Lab 2.tar then gunzip -r Lab2.
10. Gzip is used to compress files or compress directaries that are tarball already.
Tar is used to make directories into a tar ball so it can be compressed.
